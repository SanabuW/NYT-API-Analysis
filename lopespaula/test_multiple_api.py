def get_api(api):
    x
    x
    x
    x
    x

    return results



api_list = ['viewed_api', 'emailed_api', 'shared_api']

dict_api = {}
for api in api_list:
    dict_api[api] = get_api(api)

document_list = []

documentlist.append(get_mostpopular())
...